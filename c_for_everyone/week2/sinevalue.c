/*
 week 2 assignment 
 prints the sine function for an input x between (0,1)
*/

#include<stdio.h>
#include<math.h>
int main(void){
    Value = (0, 1);
    result = sin(x);
    printf("the sin of %f is %f \n",result);
    return 0;
}
